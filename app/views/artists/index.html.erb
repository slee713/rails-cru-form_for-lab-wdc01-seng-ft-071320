<h1>List of Artists</h1>

<ul>
    <% @artists.each do |artist| %>
        <li><%= link_to artist.name, artist_path(artist) %>
    <% end %>
</ul>

<h3><%= link_to "Add Artist", new_artist_path %></h3>

